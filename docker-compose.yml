version: '2.4'
services:
  iris:
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    command: --check-caps false
    ports: 
      - "9091:1972"
      - "9092:52773"
    cpuset: "0-3"
